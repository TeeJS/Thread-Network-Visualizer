# busybox httpd config
# MIME types (busybox defaults miss .json).
# busybox httpd has no config directive for custom response headers,
# so Cache-Control is left at client defaults.
.json:application/json
.js:application/javascript
.css:text/css
.html:text/html
.svg:image/svg+xml
.ico:image/x-icon
