# busybox httpd config
# MIME types (busybox defaults miss a few we need)
.json:application/json
.js:application/javascript
.css:text/css
.html:text/html
.svg:image/svg+xml
.ico:image/x-icon

# Prevent aggressive caching of the tiny config file so edits show up on refresh
H:Cache-Control: no-cache
