{
  "_comment": "Device names auto-populate from a python-matter-server (ws://<host>:5580/ws) via structural matching after the OTBR crawl - you normally do not need to edit this file. Override here only for nodes the matcher cannot identify (e.g. non-Matter devices). Keys are RLOC16 (e.g. '0xD800') or ExtAddress (16 hex chars)."
}
